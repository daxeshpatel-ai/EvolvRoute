# --- Live operational data (regenerated by the learning loop; never committed) ---
ledger.jsonl
ledger.dev-archive.jsonl
usage.log
DIGEST.md
router/orchestra.db
router/stopcells.json

# Synthetic seed data IS tracked so the loop runs on a fresh clone.
!*.example

# --- Python ---
__pycache__/
*.pyc
.pytest_cache/
.venv*/

# --- Local / private ---
pilot/
.DS_Store

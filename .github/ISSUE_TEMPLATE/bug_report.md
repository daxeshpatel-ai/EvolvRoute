---
name: Bug report
about: Report something that isn't working as expected
title: ''
labels: bug
assignees: ''
---

## What happened

A clear description of the behavior you saw.

## Steps to reproduce

The `delegate.sh` invocation (or other command) that triggers it:

```bash
./delegate.sh ...
```

## Expected behavior

What you expected to happen instead.

## Environment

- OS (macOS / Linux + version):
- Python version (`python3 --version`):
- Worker CLIs in use (`codex` / `agy` / `grok` / custom) and their versions:
- EvolvRoute commit (`git rev-parse --short HEAD`):

## Additional context

Logs, stack traces, or anything else that helps — redact any secrets or prompt content first.
